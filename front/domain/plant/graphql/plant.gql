query plant($id: ID!, $lang: String) {
  plant(id: $id) {
    id
    name
    species
    description
    imageUrl
    wateringIntervalSummerDays
    wateringIntervalWinterDays
    lastWateredOn
    nextDueOn
    careSheet(lang: $lang) {
      light
      humidity
      tip
    }
    wateringHistory {
      id
      wateredOn
    }
  }
}

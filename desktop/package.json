{
  "name": "verdure-desktop",
  "private": true,
  "version": "0.1.0",
  "description": "Tray launcher for verdure (local prod) with a live status light.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}

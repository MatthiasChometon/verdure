# back/, front/ and ai/ are folders of this monorepo (each keeps its own nested
# .gitignore for node_modules, .env, build output, …).

.env
# Local-only compose override (reuses machine-specific images/volumes).
docker-compose.override.yml

# Local Netlify folder
.netlify

# Build artifact de l'installeur IA (genere depuis ai/native/verdure-ai.iss)
ai/native/verdure-ai-installeur.exe
